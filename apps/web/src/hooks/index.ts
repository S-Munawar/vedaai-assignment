/**
 * Central export point for all custom Zustand hooks.
 * These hooks provide selector-based access to stores.
 */

export { useAuth } from './useAuth';
export { useNotifications } from './useNotifications';
export { useSchools } from './useSchools';
export { useAssignmentsList } from './useAssignmentsList';
